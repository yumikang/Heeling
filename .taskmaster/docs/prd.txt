# 힐링 음악 앱 PRD (Product Requirements Document)

## 📋 Document Info

| 항목 | 내용 |
|------|------|
| **Version** | 1.1 |
| **Date** | 2025-11-25 |
| **Author** | Leo |
| **Phase** | Phase 1 (Local-First MVP) |
| **Platform** | iOS, Android (React Native) |
| **Related Docs** | [SCREEN_FLOW.md](./SCREEN_FLOW.md), [BACKEND_API.md](./BACKEND_API.md) |

---

## 🎯 Product Vision

### Mission Statement

> 수면과 명상을 위한 몰입형 오디오-비주얼 경험을 제공하는 모바일 앱

### Core Value Proposition

일반 음악 앱과의 차별점:

1. **전체화면 비주얼 경험** - 앨범 커버가 아닌 몰입형 배경
2. **화면 밝기 조절** - 수면 환경에 최적화된 핵심 기능
3. **최소 UI** - 수면 방해 없는 미니멀 인터페이스
4. **로컬 우선 아키텍처** - 오프라인 완벽 지원

### Target Users

| 우선순위 | 타겟 | 핵심 니즈 |
|----------|------|-----------|
| Primary | 수면 장애가 있는 20-40대 | 수면 유도, 불면증 완화 |
| Secondary | 명상/요가 실천자 | 수련 보조, 집중 환경 |
| Tertiary | 집중력 향상 필요한 직장인 | 업무 몰입, 백그라운드 재생 |

---

## 📊 Product Strategy

### Phase 구분

```
Phase 1 (MVP - 8주) ← 현재
├─ 로컬 중심 아키텍처
├─ SQLite 기반 데이터 관리
├─ 번들된 컨텐츠 (5-10곡)
├─ Apple/Google 로그인
└─ 기본 재생 기능

Phase 2 (서버 연동 - 4주)
├─ 백엔드 API 연동
├─ 컨텐츠 동기화
├─ 사용자 데이터 클라우드 백업
└─ 분석/로깅

Phase 3 (확장 - TBD)
├─ 인앱 구매
├─ 소셜 기능
└─ 커뮤니티
```

### Business Model

**Phase 1 (현재)**
- Freemium 모델
- 1곡 무료 체험 (로그인 불필요)
- 전체 접근 → 로그인 필요
- **광고 없음**

**Phase 2**
- 무료: 제한된 컨텐츠
- 프리미엄: 전체 접근 ($9.99/월)

---

## 🔐 Authentication & Authorization

### 로그인 정책

**Phase 1 (현재):**
```
앱 시작 → 로그인 화면 → "건너뛰기" → Onboarding → 전체 기능 사용
                     └─ 로그인 버튼 탭 시 "준비 중" 토스트
```

- 로그인 화면 UI는 구현하되, 실제 연동은 Phase 2
- **건너뛰기** 버튼으로 바로 앱 사용 가능
- 모든 사용자 = Guest 모드 (전체 기능 접근)

**Phase 2 (예정):**
```
설치 직후:
└─ 1곡 무료 체험 (Guest Mode)
   - 로그인 불필요
   - 데이터 로컬 저장만
   - 동기화 없음

2곡 이상 접근 시도:
└─ 로그인 요구
   ├─ Apple Sign In (iOS 우선)
   ├─ Google Sign In (Android 우선)
   └─ 로그인 후 전체 컨텐츠 접근
```

### User Journey

> 상세 화면 설계는 [SCREEN_FLOW.md](./SCREEN_FLOW.md) 참조

```
┌─────────────────────────┐
│ Splash Screen           │
└────────┬────────────────┘
         │
         ↓
┌─────────────────────────┐     ┌─────────────────────────┐
│ Login Screen            │────>│ Onboarding              │
│ ┌─────────────────────┐ │     │ - 사용자 유형 선택       │
│ │ 🍎 Apple로 로그인    │ │     │ - 개인/비즈니스          │
│ └─────────────────────┘ │     │ - 직업/업종 선택         │
│ ┌─────────────────────┐ │     └────────┬────────────────┘
│ │ G  Google로 로그인   │ │              │
│ └─────────────────────┘ │              │
│     "나중에" (Guest)    │              │
└────────┬────────────────┘              │
         │                               │
         ↓                               ↓
┌─────────────────────────────────────────────────────────┐
│                     Main Tabs                           │
│  ┌──────────┬──────────┬──────────┬──────────┐         │
│  │   🏠     │    📚    │    ❤️    │    ⚙️    │         │
│  │   홈     │   전체    │  좋아요   │   설정   │         │
│  └──────────┴──────────┴──────────┴──────────┘         │
│                                                         │
│  Home: 배너 + 가로 스크롤 리스트 (추천, 테마별, 인기)     │
│  Library: 전체 트랙 세로 리스트 + 카테고리 필터          │
│  Favorites: 좋아요한 트랙 목록                          │
│  Settings: 앱 설정, 계정 관리                           │
│                                                         │
└─────────────────────────────────────────────────────────┘
         │
         │ (트랙 선택)
         ↓
┌─────────────────────────┐
│ Full Screen Player      │
│ - 앨범 아트워크          │
│ - 재생 컨트롤            │
│ - 화면 밝기 조절         │
│ - 수면 타이머            │
│ - 몰입 모드              │
└─────────────────────────┘
```

### Guest Mode 제한
| 기능 | Guest | 로그인 후 |
|------|-------|----------|
| 트랙 재생 | 1곡만 | 전체 |
| 좋아요 저장 | X | O |
| 재생 히스토리 | X | O |
| 설정 저장 | 로컬만 | 동기화 (Phase 2) |

### 데이터 관리

| 모드 | user_id | 저장소 | 동기화 |
|------|---------|--------|--------|
| Guest Mode | `null` | 로컬 SQLite만 | 없음 (앱 삭제 시 손실) |
| Logged In | `{provider_id}` | 로컬 SQLite | Phase 2에서 클라우드 |

---

## 🗄️ Data Architecture (Phase 1)

### 로컬 SQLite 스키마

```sql
-- 사용자
CREATE TABLE users (
  id TEXT PRIMARY KEY,           -- Apple/Google ID
  provider TEXT NOT NULL,        -- 'apple' | 'google'
  email TEXT,
  display_name TEXT,
  created_at INTEGER,
  last_login INTEGER
);

-- 트랙 메타데이터
CREATE TABLE tracks (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  artist TEXT,
  category TEXT,                 -- 'sleep' | 'meditation' | 'nature'
  duration INTEGER,              -- seconds
  audio_file TEXT NOT NULL,      -- local file path
  background_image TEXT NOT NULL,-- local file path
  recommended_brightness REAL,   -- 0.0 - 1.0
  is_free INTEGER DEFAULT 0,     -- 0 or 1 (체험 곡)
  sort_order INTEGER,
  created_at INTEGER
);

-- 재생 히스토리
CREATE TABLE play_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT,                  -- null for guest
  track_id TEXT NOT NULL,
  played_at INTEGER,
  duration_played INTEGER,       -- seconds actually played
  FOREIGN KEY (track_id) REFERENCES tracks(id)
);

-- 사용자 설정
CREATE TABLE user_settings (
  user_id TEXT PRIMARY KEY,
  default_brightness REAL,
  clock_style TEXT,              -- 'digital' | 'analog' | 'minimal'
  clock_persist INTEGER,         -- 0 or 1
  haptic_enabled INTEGER,        -- 0 or 1
  created_at INTEGER,
  updated_at INTEGER,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 앱 메타데이터
CREATE TABLE app_metadata (
  key TEXT PRIMARY KEY,
  value TEXT,
  updated_at INTEGER
);
-- 예: ('first_launch', '2025-01-25', 1234567890)
--     ('guest_plays_count', '1', 1234567890)
```

### 파일 구조

```
/assets/
├─ audio/
│  ├─ track_001.mp3
│  ├─ track_002.mp3
│  └─ track_003.mp3
│
├─ images/
│  ├─ track_001.webp
│  ├─ track_002.webp
│  └─ track_003.webp
│
└─ data/
   └─ initial_tracks.json      -- 초기 SQLite 시드 데이터
```

### initial_tracks.json 구조

```json
{
  "version": "1.0",
  "tracks": [
    {
      "id": "track_001",
      "title": "Rainy Forest",
      "artist": "Nature Sounds",
      "category": "sleep",
      "duration": 3600,
      "audio_file": "audio/track_001.mp3",
      "background_image": "images/track_001.webp",
      "recommended_brightness": 0.1,
      "is_free": 1,
      "sort_order": 1
    },
    {
      "id": "track_002",
      "title": "Ocean Waves",
      "artist": "Nature Sounds",
      "category": "meditation",
      "duration": 2400,
      "audio_file": "audio/track_002.mp3",
      "background_image": "images/track_002.webp",
      "recommended_brightness": 0.2,
      "is_free": 0,
      "sort_order": 2
    }
  ]
}
```

---

## 🎨 UI/UX Specification

### 화면 구조

```
App Flow:

┌──────────────────┐
│   Splash         │
│   (1초)          │
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│ Playlist View    │ ← 기본 화면
│ (곡 리스트)       │
└────────┬─────────┘
         │ (탭)
         ↓
┌──────────────────┐
│ Player View      │ ← 전체화면
│ (몰입 경험)       │
└────────┬─────────┘
         │ (스와이프 다운)
         ↓
┌──────────────────┐
│ Playlist View    │
└──────────────────┘
```

### Screen: Playlist View

```
구성 요소:
┌─────────────────────────────┐
│ [Header]                    │
│ "힐링 음악" + [⚙️Settings]   │
├─────────────────────────────┤
│                             │
│ [Track List]                │
│                             │
│ ┌─────────────────────────┐ │
│ │ [썸네일] Rainy Forest   │ │
│ │ Nature Sounds  30:00    │ │
│ │ ✓ 무료                  │ │
│ └─────────────────────────┘ │
│                             │
│ ┌─────────────────────────┐ │
│ │ [썸네일] Ocean Waves    │ │
│ │ Nature Sounds  40:00    │ │
│ │ 🔒 로그인 필요          │ │
│ └─────────────────────────┘ │
│                             │
└─────────────────────────────┘

인터랙션:
- 무료 트랙 탭 → 즉시 재생
- 잠긴 트랙 탭 → 로그인 모달
```

### Screen: Login Modal

```
Bottom Sheet 스타일:

┌─────────────────────────────┐
│                             │
│  더 많은 힐링 음악을          │
│  즐기려면 로그인하세요        │
│                             │
│  ┌─────────────────────────┐│
│  │ 🍎 Apple로 계속하기      ││
│  └─────────────────────────┘│
│                             │
│  ┌─────────────────────────┐│
│  │ 🔵 Google로 계속하기     ││
│  └─────────────────────────┘│
│                             │
│  무료로 전체 컨텐츠 이용     │
│                             │
│  [나중에]                   │
└─────────────────────────────┘

인터랙션:
- Apple/Google 탭 → OAuth 플로우
- 나중에 → 모달 닫기
```

### Screen: Player View

```
기본 상태:
┌─────────────────────────────┐
│                             │
│                             │
│    [전체화면 배경 이미지]     │
│                             │
│                             │
│                             │
│                             │
│                             │
│                             │
│ ────────────────────────── │
│ Rainy Forest                │ ← 작게
│ Nature Sounds               │ ← 더 작게
└─────────────────────────────┘

터치 → 시계 표시:
┌─────────────────────────────┐
│    [반투명 오버레이 30%]     │
│                             │
│       ╔═══════╗             │
│       ║ 02:34 ║             │
│       ║  AM   ║             │
│       ╚═══════╝             │
│                             │
│                             │
│                             │
│ ────────────────────────── │
│ Rainy Forest                │
└─────────────────────────────┘
(3초 후 자동 사라짐)

위로 스와이프 → 컨트롤:
┌─────────────────────────────┐
│    [반투명 오버레이 50%]     │
│                             │
│     ◉────────○────          │ ← 밝기
│     🔅         🔆           │
│                             │
│        ▶️  ⏸               │ ← 재생/일시정지
│                             │
│                             │
│ ────────────────────────── │
│ Rainy Forest                │
└─────────────────────────────┘
(아래로 스와이프 → 닫기)
```

### 제스처 맵핑

| 제스처 | 위치 | 동작 |
|--------|------|------|
| 탭 | 중앙 | 시계 표시/숨김 (토글) |
| 위로 스와이프 | 중앙 | 컨트롤 오버레이 표시 |
| 아래로 스와이프 | 컨트롤 | 컨트롤 닫기 |
| 좌로 스와이프 | 중앙 | 다음 곡 (로그인 사용자만) |
| 우로 스와이프 | 중앙 | 이전 곡 (로그인 사용자만) |
| 아래로 스와이프 | 전체 화면 | Player 닫기 (Playlist로 복귀) |
| 좌측 가장자리 스와이프 | - | 시스템 뒤로가기 (예약됨) |

---

## 🎵 Audio Playback Requirements

### 재생 엔진

```
라이브러리: react-native-track-player

필수 기능:
✅ 백그라운드 재생
✅ 잠금화면 컨트롤
✅ 로컬 파일 재생
✅ 재생/일시정지/정지
✅ 다음/이전 곡
✅ 재생 완료 시 다음 곡 자동 재생

제외 기능 (Phase 1):
❌ 진행바 (Seek)
❌ 볼륨 조절 (시스템 볼륨 사용)
❌ 재생 속도
❌ 이퀄라이저
```

### 재생 로직

| 모드 | 재생 범위 | 백그라운드 | 추가 기능 |
|------|----------|------------|-----------|
| Guest Mode | 무료 트랙 1곡만 | 지원 | 다른 곡 시도 → 로그인 모달 |
| Logged In | 전체 트랙 | 지원 | 곡 간 자동 전환, 히스토리 저장 |

---

## 🌙 Screen Brightness Management

### 밝기 조절 방식

```
구현: 앱 내 오버레이 (시스템 밝기 비조작)

이유:
1. 권한 불필요
2. 다른 앱에 영향 없음
3. iOS 심사 안전

기술:
- View with black background
- opacity: 1 - brightness (0.0 - 1.0)
- 실시간 조절 가능
```

### 밝기 프리셋

| 카테고리 | 권장 밝기 | 설명 |
|----------|----------|------|
| 수면 | 0.05 | 매우 어둡게 |
| 명상 | 0.2 | 약간 어둡게 |
| 휴식 | 0.5 | 중간 |

**사용자 조절**: 슬라이더로 0.0 - 1.0 범위, 설정 저장 (user_settings 테이블)

---

## ⏰ Clock Feature

### 라이브러리 선택

| Option | 설명 | 권장 |
|--------|------|------|
| Option A | react-native-clock - 디지털/아날로그 지원, 커스터마이징 쉬움 | ✅ 권장 |
| Option B | 직접 구현 - Date() API + Text 컴포넌트 | 간단한 경우 |

### 시계 스타일

```
Phase 1: 디지털만
┌─────────┐
│  02:34  │
│   AM    │
└─────────┘

Phase 2: 선택 가능
- 디지털
- 아날로그
- 미니멀 (숫자만)
```

### 표시 로직

| 동작 | 결과 |
|------|------|
| 기본 | 숨김 |
| 터치 | 3초간 표시 → 자동 숨김 |
| 롱프레스 | 고정 표시 (다시 터치 시 숨김) |

저장: `user_settings.clock_style`, `user_settings.clock_persist`

---

## 🖼️ Image Handling

### 라이브러리

```
react-native-fast-image (권장)

장점:
- 캐싱 자동
- webp 지원
- 성능 우수

대안:
- React Native Image (기본)
- 충분할 수 있음 (로컬 파일이므로)
```

### 이미지 사양

| 항목 | 스펙 |
|------|------|
| 포맷 | WebP (우선), PNG (fallback) |
| 해상도 | 1080x1920 (9:16 비율) |
| 파일 크기 | 200-500KB |
| 최적화 | 앱 번들에 포함, 런타임 로딩 없음 |

---

## 🔧 Settings Screen

### 설정 항목

```
Settings:

[계정]
├─ 로그인 정보 (Apple/Google)
└─ 로그아웃

[재생]
├─ 백그라운드 재생 (ON/OFF)
└─ 곡 끝나면 자동 다음 곡 (ON/OFF)

[화면]
├─ 기본 밝기
├─ 시계 스타일 (디지털/아날로그/미니멀)
└─ 시계 자동 숨김 (3초/5초/끔)

[접근성]
├─ 햅틱 피드백 (ON/OFF)
└─ 다크모드 (자동/켜기/끄기)

[정보]
├─ 버전 정보
├─ 이용약관
├─ 개인정보 처리방침
└─ 오픈소스 라이선스
```

---

## 📱 Technical Stack

### Core Technologies

| 분류 | 기술 |
|------|------|
| **Framework** | React Native 0.82+, TypeScript 5.0+ |
| **Navigation** | @react-navigation/native, @react-navigation/native-stack |
| **Audio** | react-native-track-player |
| **Database** | react-native-sqlite-storage |
| **Auth (iOS)** | @invertase/react-native-apple-authentication |
| **Auth (Android)** | @react-native-google-signin/google-signin |
| **Image** | react-native-fast-image |
| **Clock** | react-native-clock (또는 직접 구현) |
| **Gesture** | react-native-gesture-handler |
| **Safe Area** | react-native-safe-area-context |
| **Utilities** | react-native-device-info, @react-native-community/netinfo, react-native-haptic-feedback |

### 개발 도구

- ESLint + Prettier
- Jest (유닛 테스트)
- Detox (E2E 테스트, Phase 2)
- Reactotron (디버깅)

---

## 📦 App Bundle Structure

### Assets (Phase 1)

```
예상 번들 크기:

오디오: 5곡 × 10MB = 50MB
이미지: 5곡 × 500KB = 2.5MB
코드 + 라이브러리: ~20MB
──────────────────────────
총 예상: ~75MB

iOS App Store 제한: 200MB (Wi-Fi 불필요)
→ 안전 범위
```

### 최적화 전략

| Phase | 전략 |
|-------|------|
| Phase 1 | 5-10곡만 번들, WebP 이미지, 오디오 128kbps MP3 |
| Phase 2 | 번들: 체험 곡 1-2곡만, 나머지: 온디맨드 다운로드, 목표 번들: <50MB |

---

## 🔐 Security & Privacy

### 데이터 보호

| Phase | 보안 수준 |
|-------|----------|
| Phase 1 | 로컬 SQLite (암호화 없음), 민감 정보 없음 (재생 히스토리만) |
| Phase 2 | API 통신: HTTPS, 토큰: Keychain/Keystore 저장, SQLite 암호화 (SQLCipher) |

### 개인정보 처리

| 항목 | 내용 |
|------|------|
| 수집 항목 | Apple/Google ID (익명), 이메일 (선택), 재생 히스토리 |
| 사용 목적 | 계정 관리, 컨텐츠 추천 (Phase 2) |
| 보관 기간 | 계정 삭제 시 즉시 파기 |

---

## 📊 Analytics & Monitoring

### Phase 1 (로컬 로깅)

```
수집 이벤트:
- app_launch
- track_play_start
- track_play_complete
- login_success
- login_fail

저장:
- SQLite (play_history 테이블)
- 서버 전송 없음
```

### Phase 2 (서버 연동)

```
도구:
- Firebase Analytics (무료)
- Sentry (에러 추적)

추가 이벤트:
- 사용자 행동 패턴
- 크래시 리포트
- 성능 모니터링
```

---

## 🧪 Testing Strategy

### Phase 1 테스트

| 유형 | 대상 |
|------|------|
| Unit Tests | SQLite CRUD, Authentication flow, Audio playback logic |
| Integration Tests | Login → Track play, Guest → Login → Full access |
| Manual Tests | 전체 사용자 플로우, iOS/Android 각각, 다크모드, 백그라운드 재생 |

### Test Devices

| Platform | 기기 |
|----------|------|
| iOS | iPhone 14 Pro (iOS 17), iPhone SE (iOS 16), iPad Air (선택) |
| Android | Galaxy S23 (Android 14), Pixel 6 (Android 13) |

---

## 📅 Development Timeline

### Phase 1: 8주 계획

```
Week 1-2: 인프라 설정
├─ React Native 프로젝트 초기화
├─ SQLite 스키마 생성
├─ 번들 에셋 준비 (5곡)
└─ Navigation 구조

Week 3-4: 핵심 기능
├─ Playlist View
├─ Player View (기본)
├─ 오디오 재생 엔진
├─ 밝기 조절
└─ 시계 기능

Week 5-6: 인증 & 권한
├─ Apple/Google 로그인
├─ Guest vs Logged In 로직
├─ 로그인 모달
└─ 설정 화면

Week 7: 테스트 & 버그 수정
├─ 통합 테스트
├─ iOS/Android 검증
└─ 성능 최적화

Week 8: 출시 준비
├─ App Store/Play Store 리스팅
├─ 스크린샷/비디오
└─ 심사 제출
```

---

## 🚀 App Store Submission

### iOS App Store

| 항목 | 내용 |
|------|------|
| App Icon | 1024x1024 |
| 스크린샷 | 6.7", 6.5", 5.5" |
| App Preview | 영상 (선택) |
| 설명 | 영어, 한국어 |
| 개인정보 처리방침 | URL 필수 |

**Review Notes:**
```
This is a meditation and sleep music app.
Full-screen experience is intentional for
immersive relaxation. Standard iOS gestures
are fully preserved.
```

### Google Play Store

| 항목 | 내용 |
|------|------|
| Feature Graphic | 1024x500 |
| 스크린샷 | Phone, Tablet |
| 설명 | 영어, 한국어 |
| 카테고리 | Health & Fitness |
| 연령 | 전체 이용가 |

---

## 🔮 Phase 2 Preview (서버 연동)

### 백엔드 요구사항

```
API Endpoints:
- POST /auth/apple
- POST /auth/google
- GET /tracks (버전 체크)
- GET /tracks/:id
- POST /history

Database:
- PostgreSQL
- Users, Tracks, PlayHistory

Storage:
- AWS S3 (오디오/이미지)
```

### 동기화 전략

```
로컬 우선 (Offline-First):
1. SQLite에서 즉시 로드
2. 백그라운드에서 서버 체크
3. 새 컨텐츠 있으면 다운로드
4. SQLite 업데이트
```

---

## 📋 Success Metrics

### Phase 1 KPIs

| 분류 | 지표 | 목표 |
|------|------|------|
| **기술적** | 앱 크래시율 | < 1% |
| | 로딩 시간 | < 500ms |
| | 배터리 소모 | < 5%/hour |
| **비즈니스** | 로그인 전환율 | > 30% |
| | DAU/MAU | > 20% |
| | 평균 세션 시간 | > 15분 |
| **사용자 경험** | App Store 평점 | > 4.5 |
| | 백그라운드 재생 | 정상 작동 |
| | iOS/Android | 동일 경험 |

---

## 🎯 Open Questions & Decisions

### 즉시 결정 완료

| 질문 | 결정 |
|------|------|
| Q1: 초기 번들 곡 수? | 5곡 (무료 1곡 + 잠김 4곡) |
| Q2: 이미지 포맷? | WebP 우선, PNG fallback |
| Q3: 시계 스타일? | 디지털만 (Phase 1) |
| Q4: 다크모드 지원? | 자동 지원 (시스템 따름) |
| Q5: 햅틱 피드백? | 지원 (기본 ON) |

### 나중에 결정

- 인앱 구매 가격
- 프리미엄 기능 범위
- 컨텐츠 확장 전략
- 마케팅 채널

---

## 📚 Appendix

### A. OAuth Flow Details

**Apple Sign In:**
1. 버튼 탭
2. Face ID/Touch ID 인증
3. Apple ID 서버 통신
4. identityToken 수신
5. 앱 내부 user_id 생성
6. SQLite 저장

**Google Sign In:**
1. 버튼 탭
2. Google OAuth 웹뷰
3. 계정 선택
4. idToken 수신
5. 앱 내부 user_id 생성
6. SQLite 저장

### B. SQLite 초기화 로직

**앱 최초 실행 시:**
1. SQLite 파일 생성
2. 테이블 생성 (스키마)
3. initial_tracks.json 읽기
4. tracks 테이블에 시드 데이터 삽입
5. app_metadata에 초기화 완료 기록

**이후 실행:**
1. SQLite 파일 존재 확인
2. 버전 체크
3. 필요 시 마이그레이션

### C. 라이브러리 라이선스

| 라이브러리 | 라이선스 |
|------------|----------|
| react-native-track-player | Apache 2.0 |
| react-native-sqlite-storage | MIT |
| @react-navigation | MIT |
| react-native-fast-image | MIT |

**상업적 사용 가능, 귀속 표기 필요**

---

## ✅ Sign-off

| 항목 | 내용 |
|------|------|
| 작성자 | Leo |
| 날짜 | 2025-11-25 |
| Phase 1 예상 완료 | 8주 |

---

**Phase 1 개발 착수 승인**

이 PRD로 개발 진행 가능합니다.
